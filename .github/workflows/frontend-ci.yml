name: React Frontend CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
<<<<<<< HEAD
        working-directory: demo-frontend
=======
        working-directory: demo-frontend   # 👈 your project folder
>>>>>>> 5f39461136979f7b3c2683e64ea0e96a58efdf05

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Unit Tests
        run: npm test -- --watchAll=false

      - name: Build Application
        run: npm run build
